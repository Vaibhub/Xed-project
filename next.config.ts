import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["xite.xedinstitute.org", "api.xedinstitute.org","xedinstitute.org"],
  },
};

export default nextConfig;
